<controls:MetroWindow x:Class="UserApplication.Views.CreateUserWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Title="Create User" Height="450" Width="800"
                      mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Create a New User" FontSize="20" FontWeight="Bold"/>

            <TextBlock Text="Username" FontSize="16" Margin="0,10,0,0"/>
            <TextBox x:Name="NewUsernameTextBox" Width="200" Margin="0,5"/>

            <TextBlock Text="Password" FontSize="16" Margin="0,10,0,0"/>
            <PasswordBox x:Name="NewPasswordTextBox" Width="200" Margin="0,5"/>

            <Button Content="Create" Width="200" Style="{DynamicResource MaterialDesignRaisedButton}"
                    Margin="0,15,0,0" Click="CreateButton_Click"/>
        </StackPanel>
    </Grid>
</controls:MetroWindow>
